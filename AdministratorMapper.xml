<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.dlu.secondclass.Dao.AdministratorMapper">

    <insert id="register" parameterType="cn.edu.dlu.secondclass.entity.Administrator">
        INSERT INTO administrators (id, p_id, username, password, phone, email, is_delete)
        VALUES (#{id}, #{pId}, #{username}, #{password}, #{phone}, #{email}, #{isDelete})
    </insert>

    <select id="login" resultType="cn.edu.dlu.secondclass.entity.Administrator">
        SELECT * FROM administrators WHERE username = #{username} AND password = #{password}
    </select>

</mapper>
